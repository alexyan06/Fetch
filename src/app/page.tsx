// Placeholder shell. The portfolio dashboard replaces this — not a frozen file.

export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-3 p-8">
      <h1 className="text-3xl font-semibold tracking-tight">Fetch</h1>
      <p className="text-muted-foreground">
        Coverage that keeps up. Portfolio dashboard lands here.
      </p>
    </main>
  );
}
