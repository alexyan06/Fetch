"use client";

// STUB — Lane B fills this in with a Recharts line chart, anomalous points
// marked in red. Path and props are frozen (AnomalyTrendChartProps in
// src/lib/types.ts).
//
// Data source: detectAnomaly() in src/lib/ml/anomaly-detection.ts, run over a
// company's own history for one metric.

import type { AnomalyTrendChartProps } from "@/lib/types";

export function AnomalyTrendChart(props: AnomalyTrendChartProps) {
  void props;
  return null;
}

export default AnomalyTrendChart;
