# Copy to .env.local (gitignored) and fill in. Real values also go in the
# Vercel project's env vars. Never commit a filled-in copy of this file.

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Merge — one real company only (BambooHR / Zoho Books / HubSpot sandbox)
MERGE_API_KEY=
MERGE_ACCOUNT_TOKEN_HRIS=
MERGE_ACCOUNT_TOKEN_ACCOUNTING=
MERGE_ACCOUNT_TOKEN_CRM=

# Tier 2 explainability chat
ANTHROPIC_API_KEY=
